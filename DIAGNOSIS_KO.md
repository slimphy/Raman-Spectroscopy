# 전체 진단 보고서

## 결론

관찰한 증상은 하나의 오류보다 다음 세 가지가 겹칠 때 발생할 가능성이 큽니다.

1. GUI 스레드가 카메라 프레임을 최대 1초 기다림
2. 라이브뷰와 스캔 워커가 같은 DCAM 객체·캡처 버퍼·트리거 상태를 동시에 조작함
3. 전체 스펙트럼 맵을 기본 `float64` 3차원 배열로 RAM에 한 번에 생성함

첫 번째는 GUI 응답 없음, 두 번째는 네이티브 DCAM DLL 단계의 예외 또는 프로세스 종료, 세 번째는 Windows 페이지 파일 사용 증가로 인한 PC 전체 느려짐에 직접 연결될 수 있습니다. 아래 패치는 이 세 경로를 우선 차단합니다.

## 위험도별 진단

### 1. 매우 높음 — GUI 스레드의 장시간 카메라 대기

원본 라이브뷰는 50 ms 타이머에서 `grab_frame()`을 호출하며, 원본 컨트롤러의 프레임 준비 대기 제한은 1000 ms입니다. 카메라 트리거가 오지 않거나 장치가 불안정하면 Qt 이벤트 루프가 한 번에 최대 1초 멈출 수 있습니다. 타이머가 계속 실행되고 영상 자동 레벨, 1D 합산, 필터/ML 처리까지 같은 스레드에서 수행되어 UI 정지가 더 커질 수 있습니다.

수정:

- 라이브뷰에서 카메라 잠금을 얻지 못하면 즉시 반환
- 프레임 대기를 25 ms로 제한
- 중복 진입 방지 플래그 추가
- 표시 주기를 100 ms로 완화
- `autoLevels`는 최초 프레임에만 사용
- 스펙트럼 후처리는 최대 10 Hz로 제한

기대 효과:

- 카메라 무응답이 GUI를 장시간 붙잡는 현상 감소
- CPU/GPU 사용률의 짧은 폭주 감소
- 창 이동, 중지 버튼, 종료 명령의 응답성 향상

### 2. 매우 높음 — 동일 DCAM 객체의 동시 접근

원본에서 라이브뷰 타이머와 `MappingWorker`/`HomoepiWorker`가 동일한 카메라 객체를 사용합니다. 워커는 캡처 중지, 버퍼 해제, 트리거 변경, 캡처 재시작을 수행하는 동안 GUI가 동시에 프레임을 요청할 수 있습니다. Python 객체 수준의 예외뿐 아니라 DCAM 네이티브 라이브러리에서 비정상 종료가 발생할 수 있는 구조입니다.

수정:

- 모든 DCAM 호출을 하나의 `RLock`으로 직렬화
- 스캔 워커의 `begin_exclusive_capture()` / `end_exclusive_capture()` 도입
- 스캔 중 라이브뷰 프레임 요청은 대기하지 않고 `None` 반환
- 캡처/버퍼 할당 상태를 명시적으로 추적하고 시작·중지를 멱등 처리
- ROI와 트리거 변경은 캡처를 안전하게 중지한 뒤 적용

기대 효과:

- `cap_stop`, `buf_release`, `wait`, `copyframe`의 경쟁 상태 제거
- 간헐적 Python 프로세스 종료와 DCAM BUSY/버퍼 오류 가능성 감소
- 스캔 종료 후 라이브뷰 복귀의 일관성 향상

### 3. 매우 높음 — 전체 스펙트럼 배열의 RAM/페이지 파일 폭증

원본은 `np.full(shape_3d, np.nan)`으로 dtype을 지정하지 않아 `float64`를 사용합니다. 예를 들어 1000×1000 포인트에 스펙트럼 길이 2304이면 약 17.17 GiB가 필요합니다. 여기에 화면용 레이어, 처리 복사본, CSV 변환, Python/Qt/ML 메모리가 추가되면 Windows가 페이지 파일로 밀어내면서 PC 전체가 사실상 멈춘 것처럼 보일 수 있습니다.

수정:

- 원시 큐브를 `float32`로 저장하여 용량을 절반으로 감소
- 설정된 RAM 한도(기본 512 MiB)를 넘으면 `np.memmap`으로 디스크 저장
- 필요한 디스크 여유 공간과 20% 또는 2 GiB의 예비 공간을 사전 검사
- 기본 32 GiB 상한을 넘어서는 실수성 대형 스캔은 명확한 오류로 중단
- 16 MiB 단위로 초기화하여 큰 임시 배열 생성을 방지

기대 효과:

- 큰 맵에서 RAM 고갈과 페이지 파일 스래싱 가능성 대폭 감소
- 사용 메모리가 설정 상한 근처에서 예측 가능해짐
- 디스크 용량 부족을 스캔 중간이 아니라 시작 시 감지

대가:

- 디스크 매핑으로 전환된 경우 저장장치 속도만큼 느려질 수 있습니다.
- `raman_cache`의 `.mmap` 파일을 정상 종료 후 정리해야 합니다.

### 4. 높음 — 워커 스레드에서 Qt 위젯 직접 읽기

원본 워커의 `run()`에서 체크박스, 스핀박스, 선형 영역, x축 등 GUI 객체를 직접 읽습니다. Qt GUI 객체는 생성된 GUI 스레드에서만 다루는 것이 원칙이며, 측정 중 사용자가 설정을 바꾸면 일관되지 않은 값이나 드문 크래시가 발생할 수 있습니다.

수정:

- 워커 생성 시 GUI 스레드에서 노출·속도·트리거·x축·온도 영역을 순수 Python/NumPy 데이터로 스냅샷
- 온도 계산은 Qt 객체를 참조하지 않는 불변 데이터 클래스로 수행

기대 효과:

- 측정 중 설정 변화에 의한 데이터 경쟁 감소
- Qt 관련 간헐 오류 가능성 감소
- 한 번 시작한 스캔의 설정 재현성 향상

### 5. 높음 — 종료 시 리소스 해제 순서 부재

원본에서 명시적인 `closeEvent` 또는 애플리케이션 종료 훅이 확인되지 않았습니다. 작업 스레드나 라이브 타이머가 실행되는 동안 카메라/시리얼 객체가 파괴되면 프로그램 종료 지연, 다음 실행의 장치 BUSY, COM 포트 점유가 생길 수 있습니다.

수정:

1. GUI 타이머 중지
2. 워커에 정지 요청 후 제한 시간 대기
3. 스테이지 정지와 시리얼 닫기
4. 카메라 캡처 중지·버퍼 해제·장치 닫기·DCAMAPI 해제

기대 효과:

- 창을 닫을 때 멈추는 현상 감소
- 재실행 시 카메라 BUSY/COM 점유 가능성 감소
- 예외 종료 분석을 위한 로그 보존

### 6. 높음 — ML 모델의 동시 추론과 CUDA 오류 전파

라이브뷰와 맵 워커가 하나의 ML 프로세서를 공유할 수 있으나 원본에는 추론 잠금이 없습니다. 빠른 GUI 업데이트마다 텐서 생성·장치 전송·추론·CPU 복사가 반복되면 GPU/CPU 부하가 커지고, CUDA OOM 또는 드라이버 오류가 GUI까지 전파될 수 있습니다.

수정:

- 모델 로딩과 추론을 단일 잠금으로 직렬화
- `torch.inference_mode()` 사용
- `model.eval()`과 gradient 비활성화 유지
- 최신 PyTorch에서는 `weights_only=True`로 가중치만 로딩
- CUDA 오류/OOM 시 1회 CPU로 전환하여 재시도
- NaN/Inf 입력 방어

기대 효과:

- 동일 모델의 동시 접근 제거
- 추론 오버헤드와 메모리 사용 감소
- CUDA 문제가 전체 앱 종료로 이어질 가능성 감소

### 7. 중간 — 시리얼 송신 버퍼 초기화와 좌표 캐시

원본은 명령마다 입력·출력 버퍼를 초기화합니다. 출력 버퍼 초기화는 아직 전송 중인 바이트를 버릴 수 있습니다. 또한 논리 좌표를 실제 전송 성공보다 먼저 갱신하면 장치 위치와 소프트웨어 표시가 어긋날 수 있습니다.

수정:

- 하나의 잠금이 연결, 쓰기, 응답 대기, 종료 전체를 보호
- 명령 전에는 오래된 입력만 제거하고 출력 버퍼는 초기화하지 않음
- `time.monotonic()` 기반의 제한된 ACK 대기
- 실제 쓰기 성공 후에만 캐시 좌표 갱신
- 시뮬레이션 모드는 `pyserial` 없이도 검사 가능

기대 효과:

- 명령 손실·중복·포트 닫기 중 쓰기 가능성 감소
- 스테이지 표시 위치와 실제 명령의 일관성 향상

### 8. 중간 — 예외가 기록되지 않음

일부 작업은 `finally`만 있고 상위 예외를 파일로 남기지 않습니다. 네이티브 충돌은 Python 로그 없이 끝날 수 있지만, 그 전에 발생한 반복 프레임 실패·CUDA 오류·시리얼 예외가 남으면 원인 분리에 도움이 됩니다.

수정:

- 5 MiB × 5개 회전 로그
- 메인 스레드와 Python 스레드의 미처리 예외 훅
- 카메라, 스테이지, ML, 워커, 종료 단계별 로그

기대 효과:

- “가끔 꺼짐”을 재현하지 못해도 직전 상태를 추적 가능
- DCAM/스테이지/ML 중 어느 계층인지 빠르게 구분 가능

### 9. 중간 — 사용자 수식의 `eval`

원본 수식 기능은 제한된 builtins로 `eval`을 호출하지만, 입력식 오류가 전부 조용히 NaN으로 바뀌고 안전성·진단성이 낮습니다.

수정:

- AST 기반으로 숫자, 채널값, 기본 산술, 제한된 함수만 허용
- 과도한 거듭제곱 제한
- 실패 시 로그 기록

기대 효과:

- 잘못된 수식이 앱 상태에 영향을 주는 범위 축소
- 수식 오류의 원인 확인 가능

### 10. 중간 — 의존성 버전 미고정

`requirements.txt`가 패키지명만 포함하면 PC마다 설치 시점에 다른 PyQt6, NumPy, SciPy, PyTorch가 설치될 수 있습니다. 특히 Python 3.13, CUDA, PyTorch 조합은 버전에 따라 동작이 달라질 수 있습니다.

권장:

정상 동작하는 측정 PC에서 다음을 저장해 동일 환경을 재현하십시오.

```bat
python --version > environment.txt
python -m pip freeze >> environment.txt
nvidia-smi >> environment.txt
```

## 아직 남는 위험

- DCAMAPI/CoaXPress 드라이버 또는 보드 펌웨어 수준의 충돌은 Python 잠금만으로 완전히 제거할 수 없습니다.
- 하드웨어 트리거의 `exposure + 0.003` 보정과 고정 `sleep`은 장비별 실측으로 다시 맞춰야 합니다.
- 대형 CSV 전체 스펙트럼 저장은 매우 느리고 파일이 커집니다. 장기적으로 HDF5/Zarr와 청크 저장으로 바꾸는 편이 낫습니다.
- 워커가 `map_layers` 배열을 갱신하는 동안 GUI가 읽는 구조는 남아 있습니다. 현재는 NumPy 원소 단위 갱신과 행 완료 신호로 위험을 낮췄지만, 최종 구조는 “카메라 전용 acquisition worker + 신호/큐”가 더 안전합니다.
- Windows가 완전히 멈추거나 재부팅되는 경우에는 Python보다 카메라 드라이버, CoaXPress 보드, GPU 드라이버, 전원/PCIe 문제도 함께 확인해야 합니다.

## 장기 구조 권장안

최종적으로는 다음 구조가 가장 안정적입니다.

- GUI 스레드: 표시와 사용자 입력만 담당
- CameraWorker 한 개: 모든 DCAM 호출 독점
- StageWorker 한 개: 모든 시리얼 호출 독점
- ProcessingWorker: NumPy/ML 처리
- 각 계층은 bounded queue와 Qt signal로만 통신
- 원시 데이터는 HDF5/Zarr에 청크 단위로 즉시 저장

이번 패치는 기존 구조와 UI를 크게 바꾸지 않고 우선 치명적인 경쟁 상태와 메모리 폭증을 막는 실용적 1차 안정화입니다.
